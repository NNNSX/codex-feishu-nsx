/**
 * Bridge system types — shared across all bridge modules.
 *
 * The bridge connects Feishu to local Codex sessions.
 */
// ── Config ─────────────────────────────────────────────────────
/** Platform-specific message length limits */
export const PLATFORM_LIMITS = {
    feishu: 30000,
};
//# sourceMappingURL=types.js.map